import React from 'react'

let registerValidator = (state) => {
    const {errors, ...values} = state;
    if(values.password !== values.v_password){
        
    }

}

export default registerValidator;